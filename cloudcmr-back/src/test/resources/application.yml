firebase:
  projectId: truaro-test-gcp

cors.configuration.allowOrigins: http://localhost:8088

spring:
  cloud:
    gcp:
      datastore:
        namespace: localtest

server:
  error:
    include-message: always